// ---------------------------------------------------------------------------
// Стык оболочки с движком конструктора.
//
// Оболочка собиралась параллельно с движком, по общему письменному договору, и
// до слияния опиралась на манекен. Теперь здесь настоящий движок: договор
// оказался достаточным — ни одной правки в оболочке не понадобилось.
// ---------------------------------------------------------------------------

export * from '../editor2/api';
