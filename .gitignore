node_modules/
*.log
.DS_Store
.vite/
*.tsbuildinfo
__pycache__/
*.pyc
# dist/ is intentionally committed for HACS releases — do NOT ignore it.

# Never ship real home plans (privacy)
examples/durmon*.json

# Local scratch harnesses (kept on disk, never shipped)
_scratch*

# Автопроверки: временные копии бандла и отчёты Playwright
tests/.tmp/
test-results/
playwright-report/

# Служебные файлы инструментов, в продукт не входят
.claude-flow/
